# ME:ROOM 기능 보존 기준

이 파일은 이후 수정에서 기존 핵심 동작을 실수로 제거하지 않기 위한 기준이다.

## 세 Persona의 역할

- **SELF**: 내가 생각하는 현재의 나. 최초 입력으로 생성되고 `오늘의 한 조각` 기록에 따라 조금씩 변한다.
- **MIRROR**: 친구들이 보는 나. 친구 응답 **3개에서 최초 입주**하며, 3개는 종료 조건이 아니다. 4개, 5개 이후 응답도 계속 누적되어 재분석된다.
- **IDEAL**: 내가 되고 싶은 나. 시간에 따라 자동으로 변하지 않고 사용자가 `IDEAL 다시 만들기`를 선택했을 때만 갱신한다.

## 점수 / AI 원칙

- 공통 지표는 관계 에너지, 감정 표현, 주도성, 공감성, 안정성, 실행력의 6개이며 모두 0~100이다.
- 일치도는 AI가 만들지 않고 앱 코드의 `similarity()`가 계산한다.
- SELF / IDEAL / MIRROR의 title, summary, keywords, scores는 서버측 OpenAI 분석을 사용할 수 있다.
- OpenAI API Key는 모바일/웹 클라이언트 코드에 절대 포함하지 않는다.
- OpenAI 장애 시에도 기존 deterministic 분석으로 핵심 사용자 흐름은 유지한다.
- DAILY에서 SELF 점수 변화는 코드에서 지표당 **최대 ±3**으로 강제한다.
- MIRROR AI 점수는 친구 응답의 실제 누적 평균을 중심으로 제한해 새 응답 하나 때문에 과도하게 흔들리지 않게 한다.

## 반드시 유지할 기존 사용자 흐름

1. 회원가입 / 로그인 / 로그아웃
2. SELF 생성 및 저장
3. IDEAL 생성 및 저장
4. 재로그인 시 SELF/IDEAL/MIRROR 복원
5. ROOM 기존 디자인 / 캐릭터 애니메이션 / 캐릭터 클릭 이동
6. 친구 설문 생성 및 응답 수 표시
7. 0~2명 MIRROR 잠금
8. 3명 MIRROR 최초 입주
9. 4명 이상도 같은 설문으로 계속 응답 가능
10. MIRROR 입주 후에도 `세 가지 나 비교하기`, `친구에게 MIRROR 더 물어보기`, `오늘의 한 조각 기록` 사용 가능
11. SELF / MIRROR / IDEAL 비교 화면
12. 외부 친구는 웹 링크에서 앱 설치/로그인 없이 설문 가능

기능 추가 시 위 흐름을 대체하거나 삭제하지 않고 그 위에 확장한다.
